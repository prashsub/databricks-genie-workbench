# Pasteable prompt for Databricks Polly Omnigent

The following block is one self-contained implementation prompt. Its repository URL is an instruction to clone application source, not an operational version ledger.

```text
You are Databricks Polly Omnigent. Implement Genie Agent API-observed version control and governed promotion in databricks-genie-workbench. Write production code and tests, module by module with strict TDD, not another design proposal.

RUNTIME / MODEL
Run on model databricks-gpt-6-astra ("Astra"). Every subagent, task, or worker you spawn must also run on databricks-gpt-6-astra — do not fall back to or substitute any other model. If databricks-gpt-6-astra is unavailable in this deployment, STOP and report the routing failure rather than silently using a different model. (Model selection may additionally need to be set in Polly's launch configuration; this directive is authoritative for any model selection Polly itself controls.)

CHECKOUT AND AUTHORITATIVE INPUTS
1. Clone https://github.com/prashsub/databricks-genie-workbench.git into a fresh working directory and check out the existing branch version-control-cicd. Fetch that branch explicitly if needed. Do not overwrite unrelated working changes or silently substitute another branch.
2. Read the repository AGENTS.md and every applicable nested AGENTS.md, including the optimizer package instructions.
3. Read fully:
   - docs/design/version_control/README.md
   - docs/design/version_control/ADR-001-api-observed-versioning.md
   - docs/design/version_control/implementation_plan/README.md
   - docs/design/version_control/implementation_plan/contracts.md
   - docs/design/version_control/implementation_plan/testing-strategy.md
   - every module-NN-*.md in that implementation_plan directory.
   If a required design/plan file is missing, stop and report the missing path. Do not invent a replacement design.
4. Use the corrected design, not older docs. Where residual shorthand disagrees, the detailed protocol and ADR decision 8 govern: reserve without write authority, commit immutable evidence, then one admission CAS binding that already-committed evidence. Never assume a cross-table transaction. V1 assumes no usable conditional update and retains two PATCHes regardless of preview ETag APIs. At most one optimizer-origin champion version per run does not prohibit extra external/recovery evidence.

REPOSITORY LANDMARKS
Backend services include auth.py, config_fingerprint.py (canonicalizer to EXTEND), genie_client.py (GET and PATCH), create_agent.py/create_agent_session.py/create_agent_tools.py, lakebase.py/gso_lakebase.py, uc_client.py. Backend routers include create.py, spaces.py and auto_optimize.py (existing drift checks). Backend also has main.py, models.py and genie_creator.py. Backend tests include test_config_fingerprint.py, test_genie_client.py, test_current_version.py, test_auto_optimize_router.py and test_create_agent.py. Frontend is React/TypeScript/Vite/vitest under frontend/. Optimizer runtime/tests and its current DAB Job are under packages/genie-space-optimizer/. Root databricks.yml/app.yaml and scripts/deploy.sh/deploy_lib/preflight.sh/grant_permissions.py are provisioning integration points. Verify actual symbols and locate optimizer revert.py and startup run_as self-healing before changing them.

NONNEGOTIABLE DATabricks-NATIVE CONSTRAINTS
- Genie GET read-back is live-state authority; submitted payloads/API success responses are not observed versions.
- Unity Catalog Delta is the durable fact store AND sole v1 coordination authority. Four logical tables: genie_space_versions, genie_space_registry, genie_space_operations and genie_ops_coordination. Immutable facts are physically/grant-separated from mutable coordination; approval/release facts live durably in Delta. Enforce append-only properties and effective grants.
- DABs provision app, Jobs, schemas, tables, Volumes, grants, identities and configuration only. Never manage governed Genie content through resources.genie_spaces or bundle deploy. Detect bundle dual authority and prevent sanctioned conflicting deploys.
- Databricks Jobs execute governed restore, production change, reconciliation, promotion and optimizer champion apply. Everyday create/edit run in the app as the user through the SAME coordination/capture gate. Jobs take operation_id and load approved immutable requests themselves.
- Workbench is the approval/history/reconcile/promotion UI. No GitHub Actions, external runners, GitHub environment gates or Git/GitHub operational ledger. Git is allowed only for application source/docs and development collaboration.
- Lakebase coordination is deferred; existing telemetry/session use may remain, but no in-memory or Lakebase fallback may authorize writes. Never switch coordination backends automatically.
- Mutations execute target-locally with explicit verified executor identity, OAuth M2M/OBO, least privilege and explicit source/target profile selection when profiles are used. No automatic/default production profile, long-lived PAT or broad source-held target Genie-write credentials.

CONTRACT-FIRST IMPLEMENTATION AND OWNERSHIP
Use VC/1.0 from contracts.md. Materialize shared Python DTO/Protocol definitions and golden JSON fixtures under M02 ownership before parallel implementation; consumers mock ports, not unfinished internal implementations. M08 alone edits root composition/auth/build/DAB/deploy files, M04 alone edits existing backend create/edit/auto-optimize mutation callers, M09 owns frontend integration, and M10 owns optimizer package runtime except its DAB manifest (M08). Respect exact ownership lists in module plans; request a reviewed transfer/versioned contract change instead of editing someone else's files.

Parallel developers/agents are permitted where useful. Use the implementation_plan README's git-subtree overlay strategy: one exclusive owned-path overlay repo per module, common baseline SHA and contract version, separate full read-only reference checkout, isolated full scratch checkout for tests, and nonoverlapping prefixes in a separate subtree assembly repo. Export reviewed owned-path deltas back to the application root through one integrator. Do not reorganize production directories to make subtrees fit, ship subtree copies inside the app, or let multiple workers modify backend/main.py or shared tests. Record ownership manifests and reject off-manifest patches. If using a documented worktree fallback, disclose it; do not call it subtree work.

DEPENDENCY ORDER / RELEASE GATES
0. Read and inventory; freeze contracts; M02 contract scaffold and M08 root composition default all new/legacy write switches to disabled until gated.
1. Parallel foundation: M01 canonicalization, M02 ledger/registry, M03 coordination against fake fact store, M06 facts/approvals against identity fakes, M08 provision/identity checks, M09 API-faked UI. M03 consumes the OperationFacts Protocol, not M06 concrete imports.
2. Observation-only checkpoint: real M01/M02 + M03 observation-reservation subset + M04 observer + M09 history. Provision coordination early if needed to serialize opens; do not advance observed heads through an unsafe uncoordinated path. All writers remain off.
3. First WRITE-ENABLED milestone MUST include complete M03 coordination, M04 gate/create/edit/restore/observer, M05 base reconciliation/dual-authority detection, M06 local authorization and durable approval/idempotency consumption, M08 fail-closed identities/grants/Jobs and M10 champion routing. Integrate M09 local history/restore/reconcile states. Every FM-* and required-local F-* case must pass BEFORE enabling writes. Any uncovered legacy writer stays explicitly disabled, not bypassing the gate.
4. Complete M05 reconciliation actions, M06 production approval rules, M09 audit/action UI and M10 telemetry links. M07 offline packaging/mapping can develop in parallel, but enable target deployment only after reliable local capture/diff/restore/drift acceptance.
5. Integrate M07 target-local preflight/pull dispatch/deploy/receipt and M08 target provisioning, then M09 promotion UI and two-workspace E2E. Finish hardening and recovery drills.

STRICT TDD FOR EACH MODULE
For each ordered task in its module plan: write the named failing test FIRST, run it and verify a meaningful expected failure, implement the minimum to pass, then refactor with green tests. Preserve the red/green commands/results in your handoff. Do not implement a module wholesale and retrofit tests. Start with focused tests, then provider/consumer fixtures and touched existing regression suites. Use real Delta/permission/identity integration tests where required; a transactional in-memory fake does not prove Delta CAS. Use pytest for backend/package and existing vitest scripts for frontend. Verify package scripts/lockfiles before choosing commands. Don't create external CI/CD workflows.

SAFETY INVARIANTS TO IMPLEMENT AND TEST
- Exactly one coordination row per binding under serialized enrollment; Delta informational keys are not enforcement. Admission uses Serializable UPDATE-only MERGE with no INSERT branch. Verify exact attempt_id + generation + binding_revision by read-back; SP identity is not a fence.
- One UNRESOLVED attempt per binding, not one unexpired lease. Expiry quarantines; never takeover. All version-changing operations fail closed on coordination or mandatory evidence failure.
- Reserve, fresh GET, commit preimage, check reviewed base/authorization, one admission CAS binding preimage/op/distinct idempotency key and request digest/consumed approval/fence. Same key + different digest is rejection. Completed request/consumed approval history survives row reuse in append-only facts; failed publication leaves claim unresolved. Define safe same-operation read/finalization resume versus forbidden reuse/replay.
- No-op when all config/benchmark/metadata fingerprints match. Otherwise persist send intent and reassert fence before EACH PATCH. Config PATCH, checkpoint read-back, then description only if expected normalized config and approved metadata preimage still hold. Unexpected drift blocks/marks partial; field-independent completion requires explicit bound policy and stays conflicted.
- Timeout/5xx/disconnect, app-worker crash or failed post-write GET leads to applied_unverified/applied_partial plus unresolved quarantine. Disable hidden SDK mutation retries. Matching read-back or base equality does NOT prove prior request termination and never authorizes replay/second PATCH/compensation.
- Recovery requires trusted termination of the EXACT prior attempt, including Job retries/children or specified positive app-worker termination, followed by at least three stable GETs spanning longer than verified maximum Genie server-side request lifetime. Until that lifetime bound is verified, no auto-clear; audited human recovery is explicit and records risk/evidence. Lease expiry/heartbeat silence/cancel request alone is insufficient.
- Capture-on-open serializes BEFORE GET/append/head advancement. Preserve changed live state before showing reconciliation choices. Advance OBSERVED only; never approved/deployed or clear drift. Concurrent opens cannot regress/duplicate heads or mislabel an active managed checkpoint external. Persistence failure returns stale and disables actions.
- Create first registers provisional logical binding and durable admitted create intent. Lost response never triggers blind create replay or display-name matching. Audited orphan identity resolution only. Persist physical binding before follow-on edits, then first authoritative GET/version with no parent. Human rebind/tombstone revisions invalidate old approvals.
- Adopt is a new approval request; reapply needs fresh authorization bound to newly captured base. Production approvals require two distinct authorized human nonrequesters, at least one rechecked target-side by target SP, expiry at most 24 hours and immutable raw/canonical/artifact/map/render/target/base/policy/recovery inputs. Break-glass is separate-group, scoped, time-limited, reason-required and never bypasses evidence.
- Optimizer candidates use isolated optimizer-owned resources, never managed live bindings. Abandoned/unapplied runs create no version. At most one final applied optimizer-origin version per run with optimizer_run_id/champion_id; preimages/recovery evidence remain separately preserved. Retire in-process revert.py in favor of guarded Job restore/compensation.
- Promotion pins immutable source version/package/mapping; target recomputes every digest and validates both executor and intended-consumer access. Use schema-aware/exact SQL-identifier mapping; never unrestricted text replace. Target must be pre-enrolled; no target creation during v1 promotion. Separate outbound packages, target approvals and target receipts by securable, not Volume prefixes. Target-local polling/trigger discovery is explicit. Duplicate dispatch does not duplicate writes. Receipt export failure retries export only.
- Guarded compensation requires preauthorization plus fresh equality with recorded post-stage; never roll back over unrelated edits. Restore appends new lineage, never rewrites history.
- Overview badges compare policy-relevant approved/deployed state, not merely live==observed. Keep unknown/unreachable/unverified/conflicted/quarantined distinct. All user workflows remain inside Workbench.

VERIFY AT IMPLEMENTATION TIME
Use available Databricks skills and authoritative platform docs for target-specific capabilities. Validate fine-grained INSERT/UPDATE/DELETE support and Serializable on actual compute; keep writes disabled if privilege separation cannot be enforced. Verify Genie updateable metadata/full GET permission, request lifetime, positive worker termination, run_as behavior, actual revert.py path, DAB app/provisioning support and Delta Sharing Volume topology. Preview ETags, cross-table transactions and Lakebase are not prerequisites or alternate shortcuts. Don't invent test results or silently broaden privileges.

DONE / FINAL HANDOFF
Done means all ten module acceptance criteria, README first-milestone criteria and applicable failure matrix rows pass; backend, optimizer, frontend and contract regression suites are green; real Delta CAS/grant/identity and enabled-topology promotion checks are verified; all mutation entrypoints use the gate or are explicitly disabled; no external control plane or operational Git ledger is introduced. Record any unavailable platform check as an explicit blocker to affected feature enablement, not as a passing test. Include changed paths by module, red/green and final test results, deployment prerequisites, feature-gate state, recovery runbook references and remaining blockers. Do not claim production readiness based only on mocked tests, a Job success or an API response. Do not deploy to production or change unrelated resources without explicit authorization.
```
